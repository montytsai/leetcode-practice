---
title:
difficulty:
topics: []
category:
order:
source: []
platform: LeetCode
url:
status: todo
note:
date_created:
date_updated:
---

# <ID>. <Title>

## 題目說明

- <輸入與輸出目標>
- <重要條件、限制或邊界案例>

## 心得

<一句話心得，以及這次真正學到或修正的重點。>

---

## 解法一：<解法名稱>

### Intuition

<最初怎麼想到；核心邏輯、心得、容易誤解的地方，以及 KEY! 背後的觀念。>

### Approach

<依執行順序說明步驟、維持的 invariant，以及為什麼正確。>

### Complexity

**Time complexity: `O(...)`**

<定義變數，並說明時間複雜度的來源。>

**Space complexity: `O(...)`**

<說明額外資料結構或 call stack 的空間。>

### Code

```java
/**
 * <ID>. <Title>
 * Time Complexity: O(...)
 * Space Complexity: O(...)
 */
class Solution {
    // Keep the accepted logic clean.
}
```

程式碼規則：

- 完整包在 `java` code fence 裡。
- Header 必須包含題號、題名、Time Complexity、Space Complexity。
- 所有註解使用 B1 English。
- 我的原碼若有 `// KEY!`，翻成英文後保留並優先解釋。
- 保留我 AC 的實際邏輯；只整理格式與註解，不默默改成別人的版本。

### Code Review

- **Learning provenance**：對應 frontmatter 的 `status`。只在本人明說時標 `ac-solo` 或 `ac-assisted`，沒說就維持 `ac-unknown`，不代為判斷。
- **Correctness / invariant**：說明為什麼正確，或指出真實 bug。
- **Strength**：具體指出寫得好的地方。
- **Bug / Trade-off / Style**：分清類型；沒有問題就明說，不為了湊欄位硬挑錯。
- **Edge cases**：只寫這份提交真正需要留意的邊界。

---

## 解法二：<解法名稱>

<重複完整的 Intuition、Approach、Complexity、Code、Code Review。後續不同 AC 解法依序增加。>

---

## 解法比較

| 解法 | Time | Space | 優點 | Trade-off | 使用時機 |
| --- | --- | --- | --- | --- | --- |
| 解法一 | `O(...)` | `O(...)` | ... | ... | ... |
| 解法二 | `O(...)` | `O(...)` | ... | ... | ... |

### Optimality

<用時間、空間、可讀性或面試價值等明確指標判斷最佳性。至多建議一個有價值的替代法；除非我要求，否則不貼完整替代程式碼。>

## 相關

- <Topic 顯示名稱> — <這篇題解在此主題練到什麼，一句話>
- <topics 陣列每個項目各一行，連到對應的主題筆記，例如 `Two Pointers` → `[two-pointers](../../topics/T02-21-two-pointers.md)`>
- [LeetCode 刷題總覽](../../_moc.md)
